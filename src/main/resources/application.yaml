spring:
  application:
    name: FlightAviationApp
    
  profiles:
    active: local  # Spring будеть искать файл application-local.yaml
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080